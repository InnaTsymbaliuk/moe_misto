@charset "utf-8";

/* ImageFlow Settings
==================================== */
@media screen, projection {
  .imageflow {
    overflow: hidden;
    position: inherit;
    text-align: left;
    visibility: hidden;
    width: 100%;
    height: 100%;
    z-index: 2002;
    img {
      border: 2px solid #FFFFFF;
      position: absolute;
      top: 0px;
      visibility: hidden;
      -ms-interpolation-mode: bicubic;
      /* Enables bicubic image resampling for the IE7 */
    }
    p {
      margin: 0 auto;
      text-align: center;
      color: #FFF;
    }
    .loading {
      border: 1px solid white;
      height: 15px;
      left: 50%;
      margin-left: -106px;
      padding: 5px;
      position: relative;
      visibility: visible;
      width: 200px;
    }
    .loading_bar {
      background: #fff;
      height: 15px;
      visibility: visible;
      width: 1%;
    }
    .navigation {
      z-index: 10000;
    }
    .caption {
      font-weight: bold;
      font-size: 1.5em;
      color: #FFF;
      position: relative;
      text-align: center;
      z-index: 10001;
    }
    .scrollbar {
      border-bottom: 1px solid #b3b3b3;
      position: relative;
      visibility: hidden;
      z-index: 10002;
      height: 1px;
    }
    .slider {
      background: asset_url('slider.png') no-repeat;
      height: 14px;
      margin: -6px 0 0 -7px;
      position: absolute;
      width: 14px;
      z-index: 10003;
    }
    .slideshow {
      cursor: pointer;
      height: 14px;
      margin: 20px 0 0 20px;
      position: absolute;
      width: 14px;
      z-index: 10003;
      &.pause {
        background: asset_url('button_pause.png') no-repeat;
      }
      &.play {
        background: asset_url('button_play.png') no-repeat;
      }
    }
    .images {
      overflow: hidden;
      white-space: nowrap;
    }
    .button {
      cursor: pointer;
      height: 17px;
      position: relative;
      width: 17px;
    }
    .previous {
      background: asset_url('button_left.png') top left no-repeat;
      float: left;
      margin: -7px 0 0 -30px;
    }
    .next {
      background: asset_url('button_right.png') top left no-repeat;
      float: right;
      margin: -7px -30px 0 30px;
    }
  }
}